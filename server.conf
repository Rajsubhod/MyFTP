# FTP Server Configuration File

# Server settings
port=21
max_clients=10
buffer_size=8192

# Directory settings
ftp_root=/tmp/myftp_root

# SSL/TLS settings
cert_file=/etc/ssl/certs/myftp.crt
key_file=/etc/ssl/private/myftp.key

# Timeout settings (in seconds)
client_timeout=300
data_timeout=60